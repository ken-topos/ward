# Build output (once ward has an implementation stack)
/target/

# Private drop zone + reference shelf — clean-room governed. Everything under
# local/ is gitignored except the top-level README (mirrors ken).
/local/*
!/local/README.md

# mootup federation runtime state (actors, keys, logs)
/.moot/

# Secrets never live in the repo
*.pem
*.key

# Editor / OS noise
*.log
.DS_Store
