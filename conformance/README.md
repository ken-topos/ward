# Ward conformance

Ward's own conformance corpus (later). Where ken's conformance pins "correct
Ken," Ward's will pin the seam and engine contracts: that the export consumer
enforces I1–I5, that `compile` is faithful, that no discharge promotes to
`proved`, and that the discharge attestation binds to the export hash. Empty
until the design stabilizes enough to make executable claims.
