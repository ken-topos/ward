# Ward — design exploration

Non-normative design exploration: option analyses, candidate-stack comparisons
(Quint/Apalache, MOP variants, PBT frameworks), spikes, and narratives that
support a decision. When a decision is made it lands in `../adr/`; when a design
stabilizes it lands in `../../spec/`. Mirrors ken `docs/design/`.
